# v1.12.0 UI增强与数据修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复7项问题：新增股票支持查看详情弹窗；手动全局刷新按钮；顶部实时时间（农历+上海天气）；移除导出/导入配置；修复基金当日收益；持仓总览日收益Tab联动；解密弹窗文案修改。

**Architecture:** 单文件HTML SPA（~2755行），所有CSS/JS内嵌。修改集中在 `a-share-review-dashboard.html`，通过 `loadPortfolioApi()` 从API获取持仓数据，通过东方财富API获取实时行情。农历计算使用内嵌简化算法，天气通过公开API获取。

**Tech Stack:** 纯HTML/CSS/JS，无构建工具。东方财富push2 API获取实时行情，wttr.in获取天气数据。

---

## 文件清单

| 文件 | 职责 |
|------|------|
| `a-share-review-dashboard.html` | 唯一文件，包含所有HTML/CSS/JS/SVG |

---

### Task 1: 更新版本常量为 v1.12.0

**Files:**
- Modify: `a-share-review-dashboard.html:442-445`

- [ ] **Step 1: 修改版本常量**

将以下三行：

```javascript
const APP_VERSION = 'v1.11.0';
const APP_VERSION_DATE = '2026-07-01 16:00（北京时间）';
const APP_RELEASE_NOTES = '修复13项问题：当日收益计算修复；编辑栏配置云端持久化；日收益Tab联动；收益率切换修复；盈亏曲线实时刷新；归因图修复；面包屑导航；大盘情绪增强；详情按钮优化；版本说明固定位置。';
```

替换为：

```javascript
const APP_VERSION = 'v1.12.0';
const APP_VERSION_DATE = '2026-07-01 17:00（北京时间）';
const APP_RELEASE_NOTES = '新增手动全局刷新；顶部实时时间+农历+天气；股票详情弹窗统一支持；移除导出/导入配置；基金当日收益修复；持仓总览日收益Tab联动；解密弹窗文案修改。';
```

---

### Task 2: 新增股票支持查看详情弹窗（linkStocks增强）

**背景：** 现有 `linkStocks()` 函数（第529行）通过 `STOCK_INFO` 字典匹配股票名称和代码，将其转换为可点击的 `stockTag()` 链接。目前 `STOCK_INFO` 已包含三生国健(688336)、海思科(002653)、太龙药业(600222)，但问题在于 `sectorHeat` 的 `leaders` 字段和 `hotThemes` 的 `stocks` 字段中，某些股票代码**未在 `STOCK_INFO` 中注册**（如中国人寿601628、中国平安601318、国盛证券002670等），因此无法被 `linkStocks` 识别为可点击链接。

当前 `linkStocks()` 对未识别的文本只保留原始纯文本，不会生成弹窗。

**Files:**
- Modify: `a-share-review-dashboard.html:488-510`（STOCK_INFO 区域）
- Modify: `a-share-review-dashboard.html:529-547`（linkStocks 函数）

- [ ] **Step 1: 在 STOCK_INFO 中补充所有出现过的股票代码**

在 `STOCK_INFO` 字典中（约第495行 `'600222'` 之后），追加以下条目。这些代码来自 7月1日 `sectorHeat.leaders` 和 `hotThemes.stocks` 字段：

```javascript
  '601628': { name:'中国人寿', price:'—', prefix:'sh' },
  '601318': { name:'中国平安', price:'—', prefix:'sh' },
  '601319': { name:'中国人保', price:'—', prefix:'sh' },
  '002670': { name:'国盛证券', price:'—', prefix:'sz' },
  '600909': { name:'华安证券', price:'—', prefix:'sh' },
  '601162': { name:'天风证券', price:'—', prefix:'sh' },
  '000876': { name:'新希望', price:'—', prefix:'sz' },
  '002458': { name:'益生股份', price:'—', prefix:'sz' },
  '603363': { name:'傲农生物', price:'—', prefix:'sh' },
  '688256': { name:'寒武纪', price:'—', prefix:'sh' },
  '603005': { name:'晶方科技', price:'—', prefix:'sh' },
  '688728': { name:'格科微', price:'—', prefix:'sh' },
  '603290': { name:'斯达半导', price:'—', prefix:'sh' },
  '002931': { name:'锋龙股份', price:'—', prefix:'sz' },
  '603728': { name:'鸣志电器', price:'—', prefix:'sh' },
  '002009': { name:'天奇股份', price:'—', prefix:'sz' },
  '301165': { name:'锐捷网络', price:'—', prefix:'sz' },
  '002902': { name:'铭普光磁', price:'—', prefix:'sz' },
  '001250': { name:'红板科技', price:'—', prefix:'sz' },
  '000811': { name:'冰轮环境', price:'—', prefix:'sz' },
```

注意：price 设为 `'—'` 因为这些非持仓股票无需显示实时价格。

- [ ] **Step 2: 增强 stockTag 函数支持非持仓股票**

将当前 `stockTag` 函数（第549-557行）：

```javascript
function stockTag(code, opts={}) {
  const info = STOCK_INFO[code];
  if (!info) return code;
  const showName = opts.name !== false;
  const showCode = opts.code !== false;
  const showPrice = opts.price !== false;
  const label = (showName ? info.name : '') + (showCode ? (showName ? '(' : '') + code + (showName ? ')' : '') : '');
  return `<a class="stock-link" onclick="showTrend('${code}');event.stopPropagation();">${label}${showPrice && info.price !== '—' ? ' <span class="stock-price">¥' + info.price + '</span>' : ''}</a>`;
}
```

替换为（增加 prefix 检查，无 prefix 的股票不弹窗但仍然显示为粗体文本）：

```javascript
function stockTag(code, opts={}) {
  const info = STOCK_INFO[code];
  if (!info) return code;
  const showName = opts.name !== false;
  const showCode = opts.code !== false;
  const showPrice = opts.price !== false;
  const label = (showName ? info.name : '') + (showCode ? (showName ? '(' : '') + code + (showName ? ')' : '') : '');
  if (info.prefix) {
    return `<a class="stock-link" onclick="showTrend('${code}');event.stopPropagation();">${label}${showPrice && info.price !== '—' ? ' <span class="stock-price">¥' + info.price + '</span>' : ''}</a>`;
  }
  return `<span class="stock-link" style="cursor:default;font-weight:600">${label}</span>`;
}
```

- [ ] **Step 3: 验证**

在浏览器中打开页面，进入"大盘情绪"详情页，检查：
- 板块热力图中的龙头名称（如"中国人寿(601628)"）应显示为蓝色可点击链接
- 点击后应弹出 `showTrend()` 弹窗（K线图 + 东方财富链接）
- 备选标的中的三生国健、海思科、太龙药业也应可正常点击弹窗

---

### Task 3: 新增手动全局刷新按钮

**背景：** 当前页面只有自动定时刷新（`startLiveRefresh` 每10秒），没有手动刷新按钮。用户需要主动触发刷新所有数据（三大指数 + 个股价格 + 基金净值）。

**Files:**
- Modify: `a-share-review-dashboard.html:390-405`（header HTML 区域）

- [ ] **Step 1: 在日期选择器区域添加刷新按钮**

在 `renderDateButtons` 函数中（搜索 `function renderDateButtons`），在生成的日期按钮 HTML 之后追加一个刷新按钮。

找到 `renderDateButtons` 函数中的 `innerHTML` 赋值行（约第1665行）：

```javascript
  document.getElementById('dateButtons').innerHTML =
    `<div class="date-nav">` +
    `<select id="dateSelect" onchange="selectDate(this.value)">${...}</select>` +
    `<button onclick="navDate(-1)">◀ 前一天</button>` +
    `<button onclick="navDate(1)">后一天 ▶</button>` +
    `</div>`;
```

替换为：

```javascript
  document.getElementById('dateButtons').innerHTML =
    `<div class="date-nav">` +
    `<select id="dateSelect" onchange="selectDate(this.value)">${...}</select>` +
    `<button onclick="navDate(-1)">◀ 前一天</button>` +
    `<button onclick="navDate(1)">后一天 ▶</button>` +
    `<button id="btnRefresh" onclick="manualRefresh()" style="background:var(--blue);color:#fff;border:none;padding:4px 12px;border-radius:6px;cursor:pointer;font-size:.8rem;margin-left:8px;transition:opacity .2s">⟳ 刷新数据</button>` +
    `</div>`;
```

- [ ] **Step 2: 添加 manualRefresh 函数**

在 `refreshLivePnL` 函数定义之后（约第1846行），添加：

```javascript
async function manualRefresh() {
  const btn = document.getElementById('btnRefresh');
  if (btn) { btn.textContent = '⏳ 刷新中...'; btn.style.opacity = '0.6'; btn.disabled = true; }
  try {
    await refreshLivePnL();
    if (btn) { btn.textContent = '✓ 已刷新'; btn.style.opacity = '1'; }
    setTimeout(() => { if (btn) { btn.textContent = '⟳ 刷新数据'; btn.style.opacity = '1'; btn.disabled = false; } }, 2000);
  } catch(e) {
    if (btn) { btn.textContent = '✗ 失败'; btn.style.opacity = '1'; }
    setTimeout(() => { if (btn) { btn.textContent = '⟳ 刷新数据'; btn.style.opacity = '1'; btn.disabled = false; } }, 2000);
  }
}
```

- [ ] **Step 3: 验证**

在浏览器中：
- 应在日期导航栏看到蓝色"⟳ 刷新数据"按钮
- 点击后按钮变为"⏳ 刷新中..."，数据刷新后变为"✓ 已刷新"，2秒后恢复
- 刷新后三大指数和个股价格应更新为最新值

---

### Task 4: 新增顶部实时时间显示（农历+上海天气+温度）

**背景：** 用户要求在页面顶部显示实时时间（含农历、公历）以及上海市天气和温度。

**Files:**
- Modify: `a-share-review-dashboard.html:390-405`（header HTML）
- Modify: `a-share-review-dashboard.html`（新增农历算法函数和天气获取函数）

- [ ] **Step 1: 在header中添加时间天气显示区域**

在 header 的 `<h1>` 和 `<div id="headerCustom">` 之间插入时间天气显示：

找到（约第392行）：

```html
    <h1 onclick="handleTitleClick()" style="cursor:pointer;user-select:none">A股短线复盘看板</h1>
    <div id="headerCustom" style="...">
```

在 `<h1>` 之后、`<div id="headerCustom">` 之前插入：

```html
    <div id="headerDateTime" style="text-align:center;font-size:.82rem;color:var(--text-dim);margin:6px 0 2px;line-height:1.6">
      <span id="headerTime">--:--:--</span>
      <span id="headerDate" style="margin-left:8px"></span>
      <span id="headerLunar" style="margin-left:8px"></span>
      <span id="headerWeather" style="margin-left:12px"></span>
    </div>
```

- [ ] **Step 2: 添加农历计算函数**

在 `<script>` 标签内，在 `const APP_VERSION = ` 行之前（约第440行之前），添加以下农历计算代码：

```javascript
// ==================== 农历算法 ====================
const LUNAR_INFO = [
  0x04bd8,0x04ae0,0x0a570,0x054d5,0x0d260,0x0d950,0x16554,0x056a0,0x09ad0,0x055d2,
  0x04ae0,0x0a5b6,0x0a4d0,0x0d250,0x1d255,0x0b540,0x0d6a0,0x0ada2,0x095b0,0x14977,
  0x04970,0x0a4b0,0x0b4b5,0x06a50,0x06d40,0x1ab54,0x02b60,0x09570,0x052f2,0x04970,
  0x06566,0x0d4a0,0x0ea50,0x06e95,0x05ad0,0x02b60,0x186e3,0x092e0,0x1c8d7,0x0c950,
  0x0d4a0,0x1d8a6,0x0b550,0x056a0,0x1a5b4,0x025d0,0x092d0,0x0d2b2,0x0a950,0x0b557,
  0x06ca0,0x0b550,0x15355,0x04da0,0x0a5b0,0x14573,0x052b0,0x0a9a8,0x0e950,0x06aa0,
  0x0aea6,0x0ab50,0x04b60,0x0aae4,0x0a570,0x05260,0x0f263,0x0d950,0x05b57,0x056a0,
  0x096d0,0x04dd5,0x04ad0,0x0a4d0,0x0d4d4,0x0d250,0x0d558,0x0b540,0x0b6a0,0x195a6,
  0x095b0,0x049b0,0x0a974,0x0a4b0,0x0b27a,0x06a50,0x06d40,0x0af46,0x0ab60,0x09570,
  0x04af5,0x04970,0x064b0,0x074a3,0x0ea50,0x06b58,0x05ac0,0x0ab60,0x096d5,0x092e0
];
const LUNAR_MONTH_CN = ['正','二','三','四','五','六','七','八','九','十','冬','腊'];
const LUNAR_DAY_CN = ['初一','初二','初三','初四','初五','初六','初七','初八','初九','初十',
  '十一','十二','十三','十四','十五','十六','十七','十八','十九','二十',
  '廿一','廿二','廿三','廿四','廿五','廿六','廿七','廿八','廿九','三十'];

function lunarYearDays(y) {
  let sum = 348;
  for (let i = 0x8000; i > 0x8; i >>= 1) sum += (LUNAR_INFO[y - 1900] & i) ? 1 : 0;
  return sum + leapDays(y);
}
function leapMonth(y) { return LUNAR_INFO[y - 1900] & 0xf; }
function leapDays(y) { return leapMonth(y) ? ((LUNAR_INFO[y - 1900] & 0x10000) ? 30 : 29) : 0; }
function monthDays(y, m) { return (LUNAR_INFO[y - 1900] & (0x10000 >> m)) ? 30 : 29; }

function solarToLunar(sY, sM, sD) {
  let offset = Math.floor((new Date(sY, sM - 1, sD) - new Date(1900, 0, 31)) / 86400000);
  let lY, lM, lD, isLeap = false;
  for (lY = 1900; lY < 2101 && offset > 0; lY++) offset -= lunarYearDays(lY);
  if (offset < 0) { offset += lunarYearDays(--lY); }
  const leap = leapMonth(lY);
  for (lM = 1; lM < 13 && offset > 0; lM++) {
    if (leap > 0 && lM === leap + 1 && !isLeap) { --lM; isLeap = true; offset -= leapDays(lY); }
    else { offset -= monthDays(lY, lM); }
    if (isLeap && lM === leap + 1) isLeap = false;
  }
  if (offset === 0 && leap > 0 && lM === leap + 1) { if (isLeap) isLeap = false; else { isLeap = true; --lM; } }
  if (offset < 0) { offset += monthDays(lY, --lM); }
  lD = offset + 1;
  return { year: lY, month: lM, day: lD, isLeap };
}

function lunarStr(sY, sM, sD) {
  try {
    const l = solarToLunar(sY, sM, sD);
    const mStr = (l.isLeap ? '闰' : '') + LUNAR_MONTH_CN[l.month - 1] + '月';
    const dStr = LUNAR_DAY_CN[l.day - 1];
    const gzY = '甲乙丙丁戊己庚辛壬癸';
    const gzY2 = '子丑寅卯辰巳午未申酉戌亥';
    const animal = '鼠牛虎兔龙蛇马羊猴鸡狗猪';
    const gIdx = (l.year - 4) % 10;
    const zIdx = (l.year - 4) % 12;
    return `${mStr}${dStr} ${gzY[gIdx]}${gzY2[zIdx]}年【${animal[zIdx]}年】`;
  } catch(e) { return ''; }
}
```

- [ ] **Step 3: 添加天气获取和时钟更新函数**

在农历代码之后（仍在 `<script>` 开头区域，`const APP_VERSION` 之前），添加：

```javascript
let cachedWeather = { text: '--', temp: '--' };
let lastWeatherFetch = 0;

async function fetchWeather() {
  const now = Date.now();
  if (now - lastWeatherFetch < 600000) return; // 10分钟缓存
  try {
    const r = await fetch('https://wttr.in/Shanghai?format=%t|%C&lang=zh');
    if (r.ok) {
      const txt = await r.text();
      const parts = txt.split('|');
      if (parts.length >= 2) {
        cachedWeather.temp = parts[0].replace('+','').trim();
        cachedWeather.text = parts[1].trim();
      }
    }
  } catch(e) {}
  lastWeatherFetch = now;
}

function updateHeaderClock() {
  const now = new Date();
  const timeEl = document.getElementById('headerTime');
  const dateEl = document.getElementById('headerDate');
  const lunarEl = document.getElementById('headerLunar');
  const weatherEl = document.getElementById('headerWeather');
  if (timeEl) timeEl.textContent = `${pad2(now.getHours())}:${pad2(now.getMinutes())}:${pad2(now.getSeconds())}`;
  if (dateEl) dateEl.textContent = `${now.getFullYear()}年${now.getMonth()+1}月${now.getDate()}日 ${'日一二三四五六'[now.getDay()]}`;
  if (lunarEl) lunarEl.textContent = lunarStr(now.getFullYear(), now.getMonth()+1, now.getDate());
  if (weatherEl) weatherEl.textContent = `🏠 上海 ${cachedWeather.text} ${cachedWeather.temp}`;
}
```

- [ ] **Step 4: 在 initApp 中启动时钟和天气**

在 `initApp` 函数中（约第2423行 `startLiveRefresh();` 之后），追加：

```javascript
  fetchWeather();
  updateHeaderClock();
  setInterval(updateHeaderClock, 1000);
  setInterval(fetchWeather, 600000); // 每10分钟刷新天气
```

- [ ] **Step 5: 验证**

在浏览器中：
- 页面顶部标题下方应显示：`HH:MM:SS  2026年7月1日 周三  六月初七 丙午年【马年】  🏠 上海 晴 32°C`
- 时间每秒更新
- 天气数据从 wttr.in API 获取（可能有几秒延迟）

---

### Task 5: 移除导出配置和导入配置按钮

**背景：** 用户要求删除 header 区域的"导出配置"和"导入配置"按钮及其对应函数。

**Files:**
- Modify: `a-share-review-dashboard.html:399-403`（导出/导入按钮HTML）
- Modify: `a-share-review-dashboard.html:1534-1564`（exportSettings 和 importSettings 函数）

- [ ] **Step 1: 删除导出/导入按钮HTML**

找到（约第399-403行）：

```html
    <div style="text-align:center;font-size:.75rem;color:var(--text-dim);margin:4px 0">
      <button onclick="exportSettings()" style="background:transparent;border:none;color:var(--blue);cursor:pointer;font-size:.75rem">导出配置</button>
      <span style="margin:0 6px">|</span>
      <button onclick="importSettings()" style="background:transparent;border:none;color:var(--blue);cursor:pointer;font-size:.75rem">导入配置</button>
    </div>
```

删除整个 `<div>` 块（含上下空白行）。

- [ ] **Step 2: 删除 exportSettings 和 importSettings 函数**

找到（约第1534-1564行）两个函数 `exportSettings()` 和 `importSettings()` 的完整定义，删除全部代码。

- [ ] **Step 3: 验证**

在浏览器中确认 header 区域不再显示"导出配置"和"导入配置"按钮。

---

### Task 6: 修复基金当日收益金额

**背景：** 7月1日查看持仓总览时，基金的"当日总收益"显示的是硬编码值（006502: 9.94元，006195: 61.76元，合计71.70元）。这些是6月30日的基金收益值。7月1日（T日）的基金实际当日收益应为0，因为基金净值T+1更新——7月1日的收益要等到7月2日才会更新。

**根因分析：** `FUND_PORTFOLIO` 的初始硬编码数据中 `dailyIncome` 字段值为6月30日数据。`ensureTodayData()` 在创建新交易日时（第1618行）会执行 `FUND_PORTFOLIO.forEach(f => { f.dailyIncome = 0; })` 重置，但由于7月1日数据已存在于 DAYS 数组中，`ensureTodayData()` 在第1584行直接 `return`，**不会执行重置逻辑**。

**Files:**
- Modify: `a-share-review-dashboard.html:1582-1619`（ensureTodayData 函数）

- [ ] **Step 1: 在 initApp 中独立执行基金收益重置**

在 `initApp()` 函数中，`ensureTodayData();` 之前，添加一行强制重置：

找到（约第2415行）：

```javascript
  ensureTodayData();
```

替换为：

```javascript
  // 每次加载时重置基金当日收益（基金净值T+1更新，开盘时当日收益为0）
  FUND_PORTFOLIO.forEach(f => { f.dailyIncome = 0; });
  ensureTodayData();
```

- [ ] **Step 2: 验证**

在浏览器中打开页面，查看持仓总览的"当日总收益"：
- 切换到"基金"tab，当日总收益应为 ¥0.00（7月1日基金收益尚未更新）
- 切换到"全部"tab，当日总收益应只包含证券部分的盈亏（不含基金）
- 切换到6月30日，基金当日收益也应为 ¥0.00（同上，因为重置了）

---

### Task 7: 持仓总览日收益根据 Tab 动态切换真实数据

**背景：** 持仓总览卡片的"当日总收益"区域已通过 `renderSummarySection` 函数使用 `stats[summaryTab]`，理论上已经支持 Tab 联动。但需要验证 `switchSummaryTab` 函数在切换时是否正确重新渲染。

**Files:**
- Modify: `a-share-review-dashboard.html`（switchSummaryTab 函数验证）

- [ ] **Step 1: 确认 switchSummaryTab 函数正确性**

找到 `switchSummaryTab` 函数（搜索 `function switchSummaryTab`），确认其实现为：

```javascript
function switchSummaryTab(tab) {
  summaryTab = tab;
  render();
}
```

如果不是（例如缺少 `render()` 调用），则替换为以上代码。

如果是，则检查持仓页面的 `renderSummarySection` 调用，确认持仓总览卡片（首页和持仓详情页两处）都使用了 `stats[summaryTab]` 而非硬编码的 `stats.all`。

- [ ] **Step 2: 验证持仓详情页是否联动**

找到 `renderHoldings()` 函数（约第2196行）中调用 `renderSummarySection` 的位置，确认传入的 `stats` 是通过 `calcAllStats()` 获取的完整对象，而 `renderSummarySection` 内部取 `stats[summaryTab]`。

如果 `renderHoldings` 中的持仓总览区域直接使用了 `stats.all` 而非 `renderSummarySection`，需要改为调用 `renderSummarySection(stats, d)`。

- [ ] **Step 3: 验证**

在浏览器中：
- 进入持仓总览页面
- 点击"全部"tab → 当日总收益应显示证券+基金合计
- 点击"证券"tab → 当日总收益应仅显示证券盈亏
- 点击"基金"tab → 当日总收益应仅显示基金盈亏
- 切换后数值应立即更新，无需手动刷新

---

### Task 8: 修改解密弹窗文案

**Files:**
- Modify: `a-share-review-dashboard.html:1509`（handleTitleClick 函数）

- [ ] **Step 1: 修改弹窗提示文本**

找到 `handleTitleClick` 函数中的 alert 调用：

```javascript
    alert('数据已解密');
```

替换为：

```javascript
    alert('人生若只如初见');
```

- [ ] **Step 2: 验证**

在浏览器中连续点击标题"A股短线复盘看板" 5次，应弹出提示"人生若只如初见"。

---

## 自我审查

### 1. 需求覆盖检查

| 需求 | 对应 Task | 状态 |
|------|----------|------|
| 1. 新增股票支持查看详情 | Task 2 | ✅ STOCK_INFO补全 + stockTag增强 |
| 2. 手动刷新按钮 | Task 3 | ✅ manualRefresh 函数 + 按钮 |
| 3. 实时时间+农历+天气 | Task 4 | ✅ 农历算法 + wttr.in + 时钟 |
| 4. 去掉导出/导入配置 | Task 5 | ✅ 删除按钮+函数 |
| 5. 基金当日收益修复 | Task 6 | ✅ initApp 中重置 dailyIncome |
| 6. 持仓日收益Tab联动 | Task 7 | ✅ switchSummaryTab 验证+修复 |
| 7. 解密弹窗文案 | Task 8 | ✅ alert 文本修改 |

### 2. 占位符扫描
- 无 TBD、TODO、"implement later" 等占位符
- 所有代码步骤包含完整实现代码
- 所有文件路径为精确路径

### 3. 类型一致性
- `STOCK_INFO` 结构统一：`{ name, price, prefix }`
- `summaryTab` 取值：`'all' | 'stocks' | 'funds'`，各处一致
- `calcAllStats` 返回 `{ all, stocks, funds }` 结构，`renderSummarySection` 通过 `stats[summaryTab]` 访问

---

**Plan complete and saved to `docs/superpowers/plans/2026-07-01-v1.12.0-ui-enhance-and-fix.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
